// Central place for contact + social links. Fill in the real values as
// they become available — every place that links out reads from here.

export const ADMIN_BOOTSTRAP_EMAIL = "REPLACE_WITH_ADMIN_EMAIL@example.com";

export const WHATSAPP_NUMBER = ""; // e.g. "15551234567" (digits only, no +)
export const WHATSAPP_URL = WHATSAPP_NUMBER ? `https://wa.me/${WHATSAPP_NUMBER}` : "";

export const SOCIAL_LINKS = {
  instagram: "",
  facebook: "",
  tiktok: "",
};

export const CONTACT_EMAIL = "hello@btcollectionllc.com";
